<UserControl x:Class="OctbitEditor.Explorer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OctbitEditor"
             Foreground="{DynamicResource Foreground}"
             Background="{DynamicResource Background}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <ResourceDictionary Source="/SearchBox.xaml"/>
    </UserControl.Resources>

    <UserControl.DataContext>
        <local:ExplorerVM/>
    </UserControl.DataContext>

    <DockPanel>
        <DockPanel DockPanel.Dock="Top" Background="{DynamicResource Header}">
            <DockPanel.Resources>
                <Style TargetType="ToggleButton" BasedOn="{StaticResource {x:Type ToggleButton}}">
                    <Setter Property="Width" Value="22"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="Margin" Value="0,0,1,0"/>
                    <Style.Resources>
                        <Style TargetType="Image">
                            <Setter Property="Width" Value="15"/>
                            <Setter Property="Height" Value="Auto"/>
                            <Setter Property="SnapsToDevicePixels" Value="True"/>
                        </Style>
                    </Style.Resources>
                </Style>
                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                    <Setter Property="Width" Value="22"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="Margin" Value="0,0,1,0"/>
                    <Style.Resources>
                        <Style TargetType="Image">
                            <Setter Property="Width" Value="15"/>
                            <Setter Property="Height" Value="Auto"/>
                            <Setter Property="SnapsToDevicePixels" Value="True"/>
                        </Style>
                    </Style.Resources>
                </Style>
            </DockPanel.Resources>
            <local:SearchBox DockPanel.Dock="Right"/>
            <ToggleButton Content="★" DockPanel.Dock="Right"/>
            <ToggleButton Content="◆" DockPanel.Dock="Right"/>
            <ToggleButton Content="■" DockPanel.Dock="Right"/>

            <Button DockPanel.Dock="Left">
                <Image Source="/Resources/Icons/left.png"/>
            </Button>
            <Button DockPanel.Dock="Left">
                <Image Source="/Resources/Icons/right.png"/>
            </Button>
            <Button DockPanel.Dock="Left">
                <Image Source="/Resources/Icons/up.png"/>
            </Button>
            <Button DockPanel.Dock="Left">
                <Image Source="/Resources/Icons/reload.png"/>
            </Button>
            <TextBox Text="{Binding SelectedFolderPath.Value}" Background="{DynamicResource Header}" BorderThickness="0" VerticalContentAlignment="Center"/>
        </DockPanel>
        <Grid DockPanel.Dock="Bottom">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" MinWidth="100"/>
                <ColumnDefinition Width="4*" MinWidth="100"/>
            </Grid.ColumnDefinitions>

            <TreeView ItemsSource="{Binding Children}" SelectedItemChanged="TreeView_SelectedItemChanged" MinWidth="100">
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate DataType="local:ExplorerItem" ItemsSource="{Binding Children}">
                        <StackPanel Orientation="Horizontal">
                            <Image Source="{Binding Icon.Value}" Width="15" Margin="3,0"/>
                            <local:EditableTextBlock Text="{Binding Name.Value}"/>
                        </StackPanel>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
                <TreeView.Resources>
                    <Style TargetType="TreeViewItem" BasedOn="{StaticResource {x:Type TreeViewItem}}">
                        <Setter Property="IsExpanded" Value="{Binding IsExpanded.Value}"/>
                        <Setter Property="IsSelected" Value="{Binding IsSelected.Value}"/>
                    </Style>
                </TreeView.Resources>
            </TreeView>

            <DockPanel Grid.Column="2" MinWidth="100" HorizontalAlignment="Stretch">
                <Border Background="{DynamicResource Header}" DockPanel.Dock="Bottom">
                    <TextBlock Text="1 item selected"/>
                </Border>
                <ListBox ItemsSource="{Binding SelectedFolder.Value.Children}" Background="Transparent" Foreground="{DynamicResource Text}" BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="local:ExplorerItem">
                            <StackPanel Orientation="Horizontal">
                                <Image Source="{Binding Icon.Value}" Width="15" Margin="3,0"/>
                                <local:EditableTextBlock Text="{Binding Name.Value}" Foreground="White"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>

            <GridSplitter Grid.Column="1" Width="8" Margin="-4,0,0,0" HorizontalAlignment="Left">
                <GridSplitter.Template>
                    <ControlTemplate TargetType="{x:Type GridSplitter}">
                        <Border  Background="Transparent">
                            <Rectangle Fill="{DynamicResource Separator}" Width="1" HorizontalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </GridSplitter.Template>
            </GridSplitter>

        </Grid>
    </DockPanel>
</UserControl>
